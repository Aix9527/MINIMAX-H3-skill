# MINIMAX-H3 Director OS Skill 4.0.0

这是在你提供的 **3.3.0** 包上做的增量升级，不是重写替换其原有能力。

## 本次优化重点

1. **从抽象原则变成可执行决策**  
   主 `SKILL.md` 新增快速参考、术语、来源优先级、P0/P1/P2 按需读取、交付契约、兼容性与反模式。

2. **把 H3 Ref2VA 限制做成硬预检**  
   `validate_plan.py` 现在会检查：
   - 图像参考 > 9；
   - 视频参考 > 3；
   - 独立音频参考 > 3；
   - 混合参考文件 > 12；
   - 独立音频作为唯一参考；
   - 单段视频/音频不在 2–15 秒；
   - 视频参考总时长 > 15 秒；
   - 音频参考总时长 > 15 秒。

3. **台词所有者与实际声音来源分离**  
   `dialogueLines` 新增可选 `voiceSourceCharacterId`。默认不写时等于 `characterId`；只有模仿、变声、录音回放等剧情明确需要时才分开。

4. **梦境真实发言成为明确模式**  
   `delivery` 新增 `dream_speech`，用于“梦境视角属于 A，但梦中 B 确实开口说话”的情况。

5. **声音档案更完整**  
   新版 `voice-consistency.md` 明确：
   - 全片稳定 `(Sx)`；
   - 声音身份与画面身份分离；
   - 当前镜只绑定实际发声者；
   - normal / excited / whisper 三段试听门禁；
   - 不用 seed 或年龄标签冒充音色锁。

6. **兼容性不再硬猜版本**  
   原优化建议中列出的部分 ComfyUI/Theodore/节点最低版本，在 3.3.0 包内没有证据。4.0.0 改为“实际安装解析器 + 官方 H3 公开规范”优先，避免出现假兼容承诺。

## 安装

先备份旧目录，然后把：

`minimax-h3-director-os/`

替换到你的 Skill 目录。Codex 常见目录：

`C:/Users/<用户名>/.codex/skills/`

安装后重新开启任务/会话，使 Skill 重新加载。

## 常用检查

计划预检：

```powershell
python scripts/validate_plan.py project.director.json `
  --input-root D:/ComfyUI/input `
  --theodore-root D:/ComfyUI/custom_nodes/ComfyUI_Theodore_Director `
  --review project.review.json `
  --output preflight.json
```

声音预检：

```powershell
python scripts/validate_voice.py project.director.json `
  --review project.review.json `
  --input-root D:/ComfyUI/input `
  --theodore-root D:/ComfyUI/custom_nodes/ComfyUI_Theodore_Director `
  --output voice-preflight.json
```

台词预检：

```powershell
python scripts/validate_dialogue.py project.director.json `
  --review project.review.json `
  --input-root D:/ComfyUI/input `
  --theodore-root D:/ComfyUI/custom_nodes/ComfyUI_Theodore_Director `
  --output dialogue-preflight.json
```

## 重要边界

- 不提供 `--theodore-root` 时，只能称“静态检查”。
- 自动脚本不能判断实际换脸、真实音色、口型、最终画质或道具是否拿错。
- `review.json` 是校验旁车，不是 MiniMax 官方 API 字段。
- `voiceSourceCharacterId` 不应该被用来“修串台”；它只描述剧情上真实的声音来源。
- 参考素材导入不等于当前镜头实际使用。
- 失败后优先修最小破损契约，再 A/B 采样、Turbo、LoRA 或二次采样。

## 本包验证结果

- 单元测试：**37 passed**
- Python 编译：通过
- Markdown 本地链接：通过
- JSON 示例/Schema：通过
- GPU 视频生成：未执行
- 声音试听：未执行
- 口型人工检查：未执行
- 本地 Theodore 原生解析：此环境未提供你的本地安装路径，因此未执行
